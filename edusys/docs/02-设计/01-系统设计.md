# FayzEdu 教育管理系统 - 系统设计文档 v1.0

> 更新时间：2026-04-03

## 1. 系统架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         前端 (Vue3)                          │
│   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │
│   │  Views  │ │ Router  │ │  Store  │ │  API    │         │
│   └─────────┘ └─────────┘ └─────────┘ └─────────┘         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端 (NestJS)                           │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                    Modules                          │   │
│   │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │   │
│   │  │  Auth  │ │  User  │ │  Role  │ │Permission│      │   │
│   │  └────────┘ └────────┘ └────────┘ └────────┘       │   │
│   └─────────────────────────────────────────────────────┘   │
│                              │                               │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                  Common Layers                      │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐            │   │
│   │  │Guards    │ │Decorators│ │Interceptors│          │   │
│   │  └──────────┘ └──────────┘ └──────────┘            │   │
│   └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据库 (PostgreSQL)                     │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│   │ sys_user │ │sys_role │ │sys_permission││sys_role_permission││
│   └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 层次 | 技术 |
|------|------|
| 前端框架 | Vue 3 + TypeScript |
| UI 组件库 | NaiveUI |
| 状态管理 | Pinia |
| HTTP 客户端 | Axios |
| 后端框架 | NestJS |
| ORM | TypeORM |
| 数据库 | PostgreSQL |
| 身份认证 | JWT + Passport |
| 密码加密 | bcrypt |

### 1.3 项目结构

```
edusys/
├── backend/                    # NestJS 后端
│   ├── src/
│   │   ├── common/             # 公共模块
│   │   │   ├── decorators/     # 装饰器
│   │   │   ├── filters/        # 异常过滤器
│   │   │   ├── guards/         # 守卫
│   │   │   └── interceptors/   # 拦截器
│   │   ├── config/            # 配置
│   │   ├── entities/          # 实体
│   │   ├── modules/           # 业务模块
│   │   │   ├── auth/          # 认证模块
│   │   │   ├── user/          # 用户模块
│   │   │   ├── role/          # 角色模块
│   │   │   └── permission/    # 权限模块
│   │   ├── app.module.ts      # 根模块
│   │   └── main.ts            # 入口文件
│   └── package.json
│
├── frontend/                   # Vue3 前端
│   ├── src/
│   │   ├── api/               # API 接口
│   │   ├── components/        # 公共组件
│   │   ├── layouts/           # 布局组件
│   │   ├── router/            # 路由配置
│   │   ├── stores/            # 状态管理
│   │   ├── utils/             # 工具函数
│   │   ├── views/             # 页面视图
│   │   ├── App.vue
│   │   └── main.ts
│   └── package.json
│
└── docs/                      # 文档
    ├── 01-需求/               # 需求文档
    ├── 02-设计/               # 设计文档
    ├── 03-开发/               # 开发文档
    ├── 04-测试/               # 测试文档
    ├── 05-部署/               # 部署文档
    ├── 06-运维/               # 运维文档
    └── 07-规范/               # 开发规范
```

## 2. 数据库设计

### 2.1 ER 图

```
┌──────────────┐       ┌───────────────────┐
│   sys_role   │       │  sys_role_permission│
├──────────────┤       ├───────────────────┤
│ id (PK)      │◄──────│ role_id (FK)       │
│ role_name    │       │ permission_id (FK) │
│ role_code    │       └───────────────────┘
│ role_desc    │              ▲
│ is_system    │              │
│ created_at   │       ┌─────┴─────┐
│ updated_at   │       │           │
└──────────────┘       ▼           ▼
      │          ┌──────────┐  ┌───────────────┐
      │          │  sys_user │  │ sys_permission│
      └─────────►│ id (PK)  │  │ id (PK)       │
                 │ username │  │ permission_name│
                 │ password │  │ permission_code│
                 │ real_name│  │ permission_desc│
                 │ role_id  │  │ created_at    │
                 │ grade_ids│  │ updated_at    │
                 │ class_ids│  └───────────────┘
                 │ student_id│
                 │ status   │
                 │ phone    │
                 │ email    │
                 └──────────┘
```

### 2.2 表结构详情

#### sys_user (用户表)

```sql
CREATE TABLE sys_user (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    real_name VARCHAR(100) NOT NULL,
    role_id BIGINT NOT NULL,
    grade_ids TEXT,
    class_ids TEXT,
    student_id BIGINT,
    status TINYINT DEFAULT 1,
    phone VARCHAR(20),
    email VARCHAR(100),
    last_login_time TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_user_username ON sys_user(username);
CREATE INDEX idx_user_role_id ON sys_user(role_id);
```

#### sys_role (角色表)

```sql
CREATE TABLE sys_role (
    id BIGSERIAL PRIMARY KEY,
    role_name VARCHAR(50) NOT NULL,
    role_code VARCHAR(50) UNIQUE NOT NULL,
    role_desc VARCHAR(255),
    is_system TINYINT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_role_code ON sys_role(role_code);
```

#### sys_permission (权限表)

```sql
CREATE TABLE sys_permission (
    id BIGSERIAL PRIMARY KEY,
    permission_name VARCHAR(50) NOT NULL,
    permission_code VARCHAR(50) UNIQUE NOT NULL,
    permission_desc VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_permission_code ON sys_permission(permission_code);
```

#### sys_role_permission (角色权限关联表)

```sql
CREATE TABLE sys_role_permission (
    id BIGSERIAL PRIMARY KEY,
    role_id BIGINT NOT NULL,
    permission_id BIGINT NOT NULL,
    CONSTRAINT fk_role FOREIGN KEY (role_id) REFERENCES sys_role(id),
    CONSTRAINT fk_permission FOREIGN KEY (permission_id) REFERENCES sys_permission(id),
    UNIQUE(role_id, permission_id)
);

CREATE INDEX idx_rp_role_id ON sys_role_permission(role_id);
CREATE INDEX idx_rp_permission_id ON sys_role_permission(permission_id);
```

## 3. 模块设计

### 3.1 认证模块 (Auth Module)

#### 功能

- 用户登录 / 登出
- JWT Token 生成与验证
- 用户信息获取

#### 核心文件

```
auth/
├── auth.module.ts       # 模块定义
├── auth.controller.ts   # 控制器
├── auth.service.ts      # 服务
├── jwt.strategy.ts      # JWT 策略
├── jwt.auth.guard.ts    # JWT 守卫
└── auth.dto.ts          # DTO 定义
```

### 3.2 用户模块 (User Module)

#### 功能

- 用户 CRUD 操作
- 用户与角色关联
- 密码加密

#### 核心文件

```
user/
├── user.module.ts
├── user.controller.ts
├── user.service.ts
├── user.entity.ts
└── user.dto.ts
```

### 3.3 角色模块 (Role Module)

#### 功能

- 角色 CRUD 操作
- 角色权限关联管理

### 3.4 权限模块 (Permission Module)

#### 功能

- 权限 CRUD 操作
- 权限列表查询

## 4. 安全设计

### 4.1 密码加密

使用 bcrypt 对密码进行哈希存储：

```typescript
import * as bcrypt from 'bcrypt';

const hashedPassword = await bcrypt.hash(password, 10);
```

### 4.2 JWT 认证

- 访问令牌有效期：24小时
- 令牌内容：用户ID、用户名、角色

### 4.3 权限验证

- 基于角色的访问控制 (RBAC)
- 接口级别权限校验

## 5. API 设计规范

### 5.1 响应格式

```typescript
// 成功响应
{
  "code": 200,
  "message": "success",
  "data": { ... }
}

// 错误响应
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

### 5.2 状态码

| 状态码 | 描述 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |